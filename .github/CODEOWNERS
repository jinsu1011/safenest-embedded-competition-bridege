# SafeNest 책임 영역별 기본 리뷰어
#
# 사람이 아니라 기기·책임 영역을 기준으로 지정한다.
# 아래 handle은 2026-08-03에 저장소 collaborator 목록으로 실재와 권한(write 이상)을
# 확인했다. 팀원이 바뀌면 이 파일과 각 영역 README의 11절을 함께 갱신한다.
#
# 나중에 매칭되는 규칙이 앞 규칙을 덮어쓴다. 따라서 포괄 규칙을 맨 위에 둔다.
#
# 2026-08-17 canonical 재구성 이후 경로를 재지정했다.
# 온디바이스 AI 컴포넌트는 루트 ondevice_ai/ 가 아니라 RaspberryPi/Ondevice_AI/ 이다.
# 소유자는 기존 규칙을 실재 경로로 옮긴 것이며, 담당자를 새로 만들지 않는다.

# 기본 리뷰어 (아래 규칙에 걸리지 않는 모든 경로)
*                                    @jinsu1011

# 온디바이스 AI — 기존 /ondevice_ai/ 소유자를 신 경로로 이전
/RaspberryPi/Ondevice_AI/            @sheepmeat @jinsu1011
# 구 경로가 실수로 되살아나도 AI 담당자에게 리뷰가 가도록 유지한다.
/ondevice_ai/                        @sheepmeat @jinsu1011

# 구 /integration/ → Pi 런타임·웹 (오케스트레이션·대시보드. AI 컴포넌트가 아님)
/RaspberryPi/Runtime/                @yuseungha @jinsu1011
/RaspberryPi/Web/                    @yuseungha @jinsu1011

# 구 /devices/* 및 /devices/esp32_node/ → ESP32
# 열화상 전용 ESP 경로는 origin/main 에 없다. @rla1729 규칙은 해당 파일이 생길 때만 둔다.
/ESP32/                              @yuseungha @jinsu1011
# 구 /devices/mmwave/ — mmWave 단독 참조 펌웨어
/ESP32/reference/mmwave_platformio/  @jinsu1011

# 하드웨어 자산 (구 /hardware/3d_models/)
/hardware/                           @yuname121 @jinsu1011

# 이력·저장소 설정
# 루트 /docs/ 와 /docs/<sensor>/ 는 재구성 후 없다. 재생성하지 않는다.
/archive/                            @jinsu1011
/.github/                            @jinsu1011

# COMPONENT_SOURCES.json 은 기본 * 소유자(@jinsu1011)에 둔다.
# AI overlay 필드는 RaspberryPi/Ondevice_AI PR과 함께 검토한다. 파일 전체를 AI 소유로 두지 않는다.
#
# RaspberryPi/LCD/ 는 COMPONENT_SOURCES에서 SUPERSEDED. 기본 * (@jinsu1011).
# research/ 는 오프라인 연구 워크스페이스이며 라이브 AI 컴포넌트가 아니다. 기본 * (@jinsu1011).
